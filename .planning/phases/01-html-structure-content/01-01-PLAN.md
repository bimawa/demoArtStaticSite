---
phase: 01-html-structure-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
  - js/main.js
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - LAY-03
  - LAY-04
  - LAY-05
  - ACC-01
  - ACC-02
  - ACC-03
  - ACC-04
  - FUN-01
  - FUN-02
  - FUN-03

must_haves:
  truths:
    - "Single-page layout renders with semantic HTML sections (header, hero, gallery, contact, footer)"
    - "Hero section displays artist name and brief introduction"
    - "Gallery section displays exactly 4 artwork placeholders with title and description"
    - "Contact section shows contact form with clear call-to-action"
    - "Footer displays copyright information"
    - "Navigation uses anchor links that scroll smoothly to each section"
    - "All artwork images have descriptive alt text"
    - "Heading hierarchy follows h1 > h2 > h3 pattern"
    - "Works in all modern browsers"
  artifacts:
    - path: "index.html"
      provides: "Single-page semantic HTML with all sections"
      exports: ["header", "nav", "main", "section#hero", "section#gallery", "section#contact", "footer"]
    - path: "css/styles.css"
      provides: "Basic styles for layout and spacing"
      min_lines: 50
    - path: "js/main.js"
      provides: "Smooth scroll and form handling (optional)"
      min_lines: 10
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "<link rel=\"stylesheet\" href=\"css/styles.css\">"
      pattern: "link.*stylesheet"
    - from: "index.html"
      to: "js/main.js"
      via: "<script src=\"js/main.js\"></script>"
      pattern: "script.*main.js"
---

<objective>
Create semantic HTML foundation with all sections and content for artist portfolio.

Purpose: Establish the structural HTML layer that will be styled in Phase 2. This includes:
- Semantic sectioning (header, nav, main, sections, footer)
- Hero with artist introduction
- Gallery with 4 artwork placeholders in featured + list layout
- Contact form ready for Formspree integration
- Navigation with smooth-scroll anchor links
- Full accessibility compliance

Output: Complete index.html with semantic structure, CSS placeholders, and basic JavaScript
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-html-structure-content/01-CONTEXT.md
@.planning/phases/01-html-structure-content/01-RESEARCH.md

**Phase requirement IDs:** LAY-01, LAY-02, LAY-03, LAY-04, LAY-05, ACC-01, ACC-02, ACC-03, ACC-04, FUN-01, FUN-02, FUN-03

**Key decisions from context:**
- Hero: name + tagline + brief artistic bio
- Gallery: Featured + List layout (1 large + 3 smaller)
- Contact form only (Formspree integration - no email displayed)
- Navigation: Simple top bar with 5 links (Home, About, Gallery, Contact, footer copyright)
- Image placeholders: Use CSS background-color with aspect-ratio

**Deferred (not in this phase):**
- Image URLs: User will provide later
- Lightbox: Phase 2+
- Social media links: Not needed for v1

**Project stack:** Pure HTML/CSS/JS, no frameworks, GitHub Pages deployment
</context>

<tasks>

<task type="auto">
  <name>task 1: Create project structure and HTML skeleton</name>
  <files>index.html</files>
  <action>
Create index.html with complete semantic HTML structure:

1. **Document structure:**
   - `<!DOCTYPE html>`
   - `<html lang="en">`
   - `<head>` with viewport meta tag (ACC-04), title, meta description
   - `<body>`

2. **Skip link (accessibility):**
   ```html
   <a href="#main" class="skip-link">Skip to main content</a>
   ```

3. **Header with navigation:**
   - `<header>` containing `<nav>`
   - Site name as logo/brand
   - Navigation links: Home (#hero), About (#hero), Gallery (#gallery), Contact (#contact)
   - Use semantic `<nav>` with `<ul>` list

4. **Main content area:**
   - `<main id="main">` wrapper
   - `<section id="hero">` for hero/about
   - `<section id="gallery">` for artwork display
   - `<section id="contact">` for contact form

5. **Footer:**
   - `<footer>` with copyright

6. **Required attributes:**
   - `lang="en"` on html element
   - Descriptive meta description
   - All interactive elements keyboard-accessible
  </action>
  <verify>
    <automated>ls -la index.html && grep -c "<!DOCTYPE html>" index.html && grep -c "<section" index.html</automated>
  </verify>
  <done>index.html exists with DOCTYPE, header, nav, main with 3 sections, footer</done>
</task>

<task type="auto">
  <name>task 2: Add hero section content</name>
  <files>index.html</files>
  <action>
Add content to hero section:

1. **h1 - Artist name:**
   ```html
   <h1>Artist Name</h1>
   ```

2. **Tagline - h2 or p with class:**
   ```html
   <p class="tagline">Visual Artist</p>
   ```

3. **Bio - brief artistic statement:**
   - 2-3 sentences about artistic focus and style
   - Use generic placeholder text for now
   - Mark as placeholders in comments

4. **Proper heading hierarchy:**
   - h1 for artist name (only one h1)
   - h2 for section titles in other sections
  </action>
  <verify>
    <automated>grep -c "<h1>" index.html && grep -E "<section id=\"hero\".*>" index.html | head -1</automated>
  </verify>
  <done>Hero section contains h1 with artist name, tagline paragraph, bio paragraphs</done>
</task>

<task type="auto">
  <name>task 3: Create gallery with 4 artwork placeholders</name>
  <files>index.html</files>
  <action>
Build gallery section with Featured + List layout:

1. **Gallery structure:**
   - Use `<figure>` for each artwork with `<figcaption>`
   - 1 featured artwork (larger)
   - 3 list artworks (smaller)
   - Exactly 4 total (LAY-03 requirement)

2. **Image placeholders:**
   - Use CSS background-color with aspect-ratio (from context: "user will provide real image URLs — use placeholder structure")
   - Include `loading="lazy"` for non-featured images
   - Use placeholder color backgrounds (e.g., #e0e0e0)

3. **Accessibility (ACC-01):**
   - Descriptive alt text for each image (artwork titles)
   - Format: "Title of artwork, year, medium"

4. **Metadata:**
   - Title in `<figcaption>` below each image
   - Year (use placeholder "2024")

5. **HTML:**
   ```html
   <section id="gallery">
     <h2>Gallery</h2>
     <div class="gallery-grid">
       <!-- Featured -->
       <figure class="featured">
         <div class="artwork-placeholder" style="background-color: #e0e0e0;" role="img" aria-label="Featured artwork"></div>
         <figcaption>
           <h3>Artwork Title</h3>
           <p>2024</p>
         </figcaption>
       </figure>
       <!-- 3 more figures -->
     </div>
   </section>
   ```
  </action>
  <verify>
    <automated>grep -c '<figure' index.html && grep -c "alt=" index.html</automated>
  </verify>
  <done>Gallery section contains exactly 4 figure elements with artwork placeholders and alt text</done>
</task>

<task type="auto">
  <name>task 4: Add contact section with form</name>
  <files>index.html</files>
  <action>
Create contact form section:

1. **Section structure:**
   - `<h2>Contact</h2>` for section title
   - Contact message/intro text
   - Form with required fields

2. **Form fields (per context: "name, email, message"):**
   ```html
   <form action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
     <div class="form-group">
       <label for="name">Name</label>
       <input type="text" id="name" name="name" required>
     </div>
     <div class="form-group">
       <label for="email">Email</label>
       <input type="email" id="email" name="email" required>
     </div>
     <div class="form-group">
       <label for="message">Message</label>
       <textarea id="message" name="message" required></textarea>
     </div>
     <button type="submit">Send Message</button>
   </form>
   ```

3. **Accessibility:**
   - All inputs have associated `<label>` elements (ACC-03)
   - Use `for` attribute linking labels to inputs
   - Required fields marked with `required` attribute

4. **Note:** Formspree form_id is placeholder - user will configure later
  </action>
  <verify>
    <automated>grep -c '<form' index.html && grep -c '<label' index.html && grep -c 'type="email"' index.html</automated>
  </verify>
  <done>Contact section contains form with name, email, message fields and proper labels</done>
</task>

<task type="auto">
  <name>task 5: Add navigation and footer</name>
  <files>index.html</files>
  <action>
Complete navigation and footer:

1. **Navigation:**
   - Top bar with site name/brand
   - Links: Home, About, Gallery, Contact
   - Anchor links to section IDs
   - Use semantic `<nav>` with unordered list

2. **Footer:**
   - Copyright notice: "© 2024 Artist Name. All rights reserved."
   - Simple text, no links (per context: no social links)

3. **Smooth scroll (FUN-01):**
   - Add CSS: `html { scroll-behavior: smooth; }`
   - This enables smooth scrolling via anchor links without JS
  </action>
  <verify>
    <automated>grep -c '<nav' index.html && grep -c '<footer' index.html && grep -c "©" index.html</automated>
  </verify>
  <done>Navigation with 4+ links, footer with copyright, smooth scroll CSS</done>
</task>

<task type="auto">
  <name>task 6: Add basic CSS and JavaScript files</name>
  <files>css/styles.css, js/main.js</files>
  <action>
Create supporting files:

1. **css/styles.css:**
   - Basic reset (margin: 0, padding: 0, box-sizing: border-box)
   - `scroll-behavior: smooth` on html
   - Skip link styling (hidden by default, visible on focus)
   - Basic section styling (min-height, padding for anchor scroll)
   - Gallery grid layout (CSS Grid for responsiveness)
   - Basic form styling
   - Footer styling

2. **js/main.js:**
   - Smooth scroll fallback (if CSS insufficient)
   - Form submission handling (optional, for Formspree)
   - Placeholder - mostly smooth scroll works via CSS
  </action>
  <verify>
    <automated>ls -la css/styles.css js/main.js 2>/dev/null && echo "Files created"</automated>
  </verify>
  <done>CSS and JS files created with basic styling and functionality</done>
</task>

</tasks>

<verification>
- [ ] index.html exists and is valid HTML5
- [ ] Semantic sections present: header, nav, main, section#hero, section#gallery, section#contact, footer
- [ ] Exactly 4 artwork placeholders in gallery
- [ ] Contact form has name, email, message fields with labels
- [ ] Navigation has anchor links to all sections
- [ ] Heading hierarchy: h1 in hero, h2 for sections
- [ ] All images have descriptive alt text
- [ ] Viewport meta tag present
- [ ] Works in modern browsers (no browser-specific features used)
</verification>

<success_criteria>
- Single-page layout renders with semantic HTML sections
- Hero section displays artist name and brief introduction
- Gallery section displays exactly 4 artwork placeholders with title and description
- Contact section shows contact form with clear call-to-action
- Footer displays copyright information
- Navigation uses anchor links that scroll smoothly to each section
- All artwork images have descriptive alt text
- Heading hierarchy follows h1 > h2 > h3 pattern
- Works in all modern browsers
</success_criteria>

<output>
After completion, create `.planning/phases/01-html-structure-content/01-01-SUMMARY.md`
</output>
